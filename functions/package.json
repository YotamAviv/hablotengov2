{
  "name": "hablotengo-functions",
  "description": "Cloud Functions for hablotengo",
  "scripts": {
    "test": "for f in test/*.test.js; do node --test \"$f\" || exit 1; done",
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "engines": {
    "node": "22"
  },
  "main": "index.js",
  "dependencies": {
    "firebase-admin": "^12.1.0",
    "firebase-functions": "^7.2.2"
  },
  "private": true
}
